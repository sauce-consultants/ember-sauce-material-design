import Ember from 'ember';
import layout from '../templates/components/smd-navigation';

export default Ember.Component.extend({
  layout,
  classNames: ['mdl-navigation'],
  tagName: 'navigation',
});

import Ember from 'ember';
import layout from '../templates/components/smd-data-list';

const {
  Component
} = Ember;

export default Component.extend({
  layout,
  classNames: ['smd-data-list'],
});

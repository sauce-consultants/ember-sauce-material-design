<div class="mdl-layout__obfuscator is-visible" {{action "outsideAction"}}></div>
<div class="smd-modal__container">
{{#if hasHeader}}
  <div class="smd-modal__header">
  {{#if hasClose}}
    {{smd-button icon="close" action="closeAction" class="smd-modal__close"}}
  {{/if}}
  {{#if hasTitle}}
    <h1 class="smd-modal__title">{{title}}</h1>
  {{/if}}
  {{#if hasAction}}
    {{smd-button text=actionText action="headingAction" class="smd-modal__action"}}
  {{/if}}
  </div>
{{/if}}
  <div class="smd-modal__body">
    {{yield}}
  </div>
{{#if hasFooter}}
  <div class="smd-modal__footer">
    {{#if hasPrimary}}
      {{smd-button text=primaryText action="primaryAction"}}
    {{/if}}
    {{#if hasSecondary}}
      {{smd-button text=secondaryText action="secondaryAction"}}
    {{/if}}
  </div>
{{/if}}
</div>

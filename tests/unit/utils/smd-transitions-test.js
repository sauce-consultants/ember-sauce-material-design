import smdTransitions from 'dummy/utils/smd-transitions';
import { module, test } from 'qunit';

module('Unit | Utility | smd transitions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = smdTransitions();
  assert.ok(result);
});

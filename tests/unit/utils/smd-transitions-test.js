import {
  tabTransitions
} from 'ember-sauce-material-design/utils/smd-transitions';
import {
  module,
  test
} from 'qunit';

module('Unit | Utility | smd transitions');

// Replace this with your real tests.
test('it works', function(assert) {
  var scope = {};
  tabTransitions(scope, []);
  assert.ok(scope);
});

{{smd-header
title="Editable Lists"
backIcon="arrow_back"
backAction="back"
scrolling=true
}}

<div class="smd-page">

    <header class="smd-page__header">
        <h1 class="smd-page__title">Editable Lists</h1>
    </header>

    <section class="smd-page__body">

        <p>Here is an example of an editable list using the items attribute.</p>

        {{#smd-editable-list title="Dummy items" items=model addItem='addDummyItem' removeItem='removeDummyItem' as |item|}}
          {{smd-form-control type='text' model=item name='someProperty' value=item.someProperty label="Some Property Label"}}
        {{/smd-editable-list}}

      {{code-snippet name="smd-editable-list.hbs"}}

        <p>Further customisation can be achieved by manually rendering smd-editable-list-items into the list and not using the items property. Here is an example.</p>

      {{#smd-editable-list title="Custom Dummy items" addItem='addDummyItem' removeItem='removeDummyItem' as |item|}}
        {{#each model as |listItem|}}
          {{#smd-editable-list-item item=listItem as |item|}}
            {{smd-form-control type='text' model=item name='someProperty' value=item.someProperty label="Some Property Label"}}
          {{/smd-editable-list-item}}
        {{/each}}
      {{/smd-editable-list}}

      {{code-snippet name="smd-editable-list-custom.hbs"}}

    </section>

    <header class="smd-page__header" id="attributes">
        <h1 class="smd-page__title">Attributes</h1>
    </header>

    <section class="smd-page__body">
        <table class="sg-table sg-table--attributes">
            <thead>
            <tr>
                <th>Attribute</th>
                <th>Description</th>
                <th>Type</th>
                <th>Default</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>title</td>
                <td>The title text for the alert box</td>
                <td><em>string</em></td>
                <td>null</td>
            </tr>
            <tr>
                <td>affirmativeText</td>
                <td>Text for the confirm button</td>
                <td><em>string</em></td>
                <td>null</td>
            </tr>
            <tr>
                <td>dismissiveText</td>
                <td>Text for the dismiss button</td>
                <td><em>string</em></td>
                <td>null</td>
            </tr>
            </tbody>
        </table>
    </section>


    <header class="smd-page__header" id="actions">
        <h1 class="smd-page__title">Actions</h1>
    </header>

    <section class="smd-page__body">
        <table class="sg-table sg-table--actions">
            <thead>
            <tr>
                <th>Action</th>
                <th>Description</th>
                <th>Params</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>action</td>
                <td>Default action called when an event is fired and a event specific action has not been defined. Possible types are: confirm & dismiss.</td>
                <td><em>type</em> (string), <em>alert</em> (object)</td>
            </tr>
            <tr>
                <td>affirmativeAction</td>
                <td>If specified this will be called on affimative action.</td>
                <td><em>alert</em> (object)</td>
            </tr>
            <tr>
                <td>dismissiveAction</td>
                <td>If specified this will be called on the dismissive action. By default this is set to false which will just hide the alert on click.</td>
                <td><em>alert</em> (object)</td>
            </tr>
            </tbody>
        </table>
    </section>

</div>
